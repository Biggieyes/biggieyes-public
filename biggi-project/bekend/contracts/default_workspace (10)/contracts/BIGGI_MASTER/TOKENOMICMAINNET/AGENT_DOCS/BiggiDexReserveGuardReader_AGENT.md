# BiggiDexReserveGuardReader
Reader for guard baseline reserve, quote token, price sanity config, cooldown, refill readiness, local price anchor, and oracle readiness. Keep interfaces synchronized with BiggiDexReserveGuard public getters, especially `quoteOracleStatus()`, `maxOracleStaleness()`, and `requireQuoteOracleForPriceCheck()`.
